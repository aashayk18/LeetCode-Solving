# Intuition
Count the number of occurrences of characters of jewels in stones.

# Approach
Use the `count()` method to calculate the number of occurrences of each character of string `jewels` in string `stones`. Sum this using the `sum()` function and return the value.

# Complexity
- Time complexity:


- Space complexity:
