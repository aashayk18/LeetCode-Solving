# Intuition
Run "for loop" across the list, check sum against the target.

# Approach
Brute Force approach to run "nested for loops", check sum against the target.

# Complexity
Time complexity:
$O(n^2)$

Space complexity:
$O(1)$
