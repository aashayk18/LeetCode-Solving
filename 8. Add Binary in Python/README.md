# Intuition
Convert given strings to binary numbers and add them.

# Approach
Convert the given input strings to binary integers, before adding them. Convert the obtained result to the binary format starting with "0b" and then the result. Return this binary format result, from the 2nd index onwards, to leave out the "0b" and only include the actual binary result.

# Complexity
Time complexity:

Space complexity:
