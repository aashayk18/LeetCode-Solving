# Intuition
Append values to an array as per the given conditions, and return it.

# Approach
Create an array with the word `"Fizz"` multiplied by the boolean expression value of the condition whether `i` is divisible by 3, which gives either `1` or `0` which means the word is either in the array or it isn't. Add the word `"Buzz"` in a similar manner, if `i` is divisible by `5`. Otherwise, just add `i` to the array. Return this array as the ouput.

# Complexity
- Time complexity:


- Space complexity:
