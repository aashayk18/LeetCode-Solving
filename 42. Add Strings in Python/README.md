# Intuition
Convert the strings to integers, add them, convert the result back to a string.

# Approach
Convert `num1` and `num2` to integers using the `int()` function. Add these two integers, and convert their result back to a string using the `str()` function before returning it.

# Complexity
- Time complexity:


- Space complexity:
