# Read from the file file.txt and output the tenth line to stdout.
sed -n '10p' file.txt

# This code uses the sed command to print the 10th line of the file. The -n option is used to suppress the default output and '10p' is used to print the 10th line of the file. 
