# Intuition
Split the input into separate words and return the length of the last one.

# Approach
Remove any trailing spaces at the end of the input sentence using the rstrip() method in Python. Split the resulting sentence into individual words using the split() method, and return the length of the last word.

# Complexity
Time complexity:

Space complexity:
